# Быстрое исправление - Инструкция

## Проблемы которые нужно исправить:

1. ❌ Vercel деплой не запускается (MIME type error)
2. ❌ Процессы в карточке культуры не показывают шаги
3. ❌ Справочники не сохраняют изменения

## ✅ ЧТО УЖЕ ИСПРАВЛЕНО:

1. ✅ Конфигурация Vercel (vercel.json) - исправлена, деплой должен работать
2. ✅ Типы TypeScript (database.types.ts) - обновлены для новых step_type
3. ✅ Создано 6 шаблонов процессов через API

## ⚠️ ЧТО НУЖНО СДЕЛАТЬ ВРУЧНУЮ:

### Шаг 1: Добавить шаги к процессам в Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard) → Ваш проект
2. Перейдите в **SQL Editor**
3. Скопируйте и выполните содержимое файла:
   ```
   supabase/migrations/1768500000_fix_process_templates.sql
   ```

   Этот файл:
   - Удалит старые шаблоны процессов
   - Создаст 6 новых шаблонов с полными шагами:
     * PROC-BM-ISOLATION-V1 (9 шагов)
     * PROC-ADIPOSE-ISOLATION-V1 (11 шагов)
     * PROC-PASSAGE-V2 (10 шагов)
     * PROC-BANKING-V2 (8 шагов)
     * PROC-THAWING-V2 (8 шагов)
     * PROC-MEDIA-CHANGE-V1 (4 шага)

### Шаг 2: Добавить RLS политики для справочников

1. В том же **SQL Editor**
2. Скопируйте и выполните содержимое файла:
   ```
   supabase/migrations/1768510000_fix_rls_policies_dictionaries.sql
   ```

   Этот файл:
   - Добавит политики INSERT/UPDATE/DELETE для authenticated пользователей
   - Исправит сохранение в справочниках:
     * container_types
     * process_templates
     * process_template_steps
     * locations
     * equipment
     * media_recipes

## 🧪 Проверка результата:

После выполнения SQL:

### 1. Проверьте процессы:
```sql
-- Должно быть 6 шаблонов
SELECT template_code, name FROM process_templates WHERE is_active = true;

-- Должно быть ~50 шагов
SELECT COUNT(*) FROM process_template_steps;
```

### 2. Проверьте справочники в UI:
- Откройте **Справочники** → **Типы контейнеров**
- Попробуйте создать новую запись
- Попробуйте изменить существующую
- Изменения должны сохраняться без ошибок

### 3. Проверьте процессы в карточке культуры:
- Откройте любую культуру
- Нажмите **"Начать процесс"**
- Выберите шаблон (например, "Пассирование")
- Должны отображаться все шаги процесса

## 📝 Текущий статус:

✅ **Через API уже сделано:**
- Создано 6 шаблонов процессов (без шагов)
- Обновлена конфигурация Vercel
- Обновлены TypeScript типы

❌ **Требуется вручную в Supabase Dashboard:**
- Добавить шаги к шаблонам процессов (60+ записей)
- Добавить RLS политики для справочников

**Почему вручную?**
- Добавление enum values и DDL операции требуют admin доступа
- REST API через anon ключ не может выполнять ALTER TYPE и CREATE POLICY
- Самый быстрый способ - выполнить SQL напрямую в Dashboard

## 🔗 Полезные ссылки:

- Supabase Dashboard: https://supabase.com/dashboard
- SQL Editor: https://supabase.com/dashboard/project/mjgogarzewxjxohvormk/sql
- Миграции: `supabase/migrations/` в проекте

## ❓ Если что-то не работает:

1. Проверьте логи в Supabase Dashboard → Logs
2. Проверьте что RLS включен на таблицах
3. Проверьте что вы вошли как authenticated пользователь в приложении
4. Проверьте консоль браузера на ошибки

Все файлы миграций находятся в папке `supabase/migrations/` в проекте.
